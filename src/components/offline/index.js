import { h } from 'preact';

const Offline = () => (
  <div class="mt-lg flex align-center justify-center">
    <h3 class="text-center mt-md">You seem to be offline...</h3>
  </div>
);

export default Offline;
